CREATE TABLE perf (
       ID INTEGER PRIMARY KEY,
       l2_reg REAL,
       learning_rate REAL,
       max_epochs INT,
       min_epochs INT,
       momentum REAL,
       n_hidden_layers INT,
       n_hidden_units INT,
       normalize INT,
       patience INT,
       REWEIGHT INT,
       AUC real
);
